APP_ENV=development
DATABASE_URL=postgresql://user:password@127.0.0.1:5432/rungoal
JWT_SECRET=change-me-to-a-random-secret
JWT_REFRESH_SECRET=change-me-to-a-different-random-secret
ADMIN_JWT_SECRET=change-me-to-another-random-secret
CORS_ORIGINS=http://localhost:5173,http://localhost:5174
# Only needed once when the Admin table is empty (password must be 12+ characters)
# ADMIN_SEED_USERNAME=admin
# ADMIN_SEED_PASSWORD=change-this-initial-password
PORT=3000
