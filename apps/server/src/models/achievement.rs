// Models for the achievement system.
//
// Achievement definitions live in `services::achievement::ACHIEVEMENTS` (code-driven,
// not database-driven). Achievement detection uses `services::stats::UserStats`.
